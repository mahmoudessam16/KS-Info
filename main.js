let subscribeEmail = document.getElementById("Form");
subscribeEmail.onsubmit = function (e) {
    e.preventDefault();
}
// Contact Form
let contactForm =  document.getElementById("contact");
contactForm.onsubmit = function (e) {
    e.preventDefault();
}